{
  "name": "soma-inquiry-poc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base": "hardhat run --network base scripts/deploy.ts",
    "deploy:basesepolia": "hardhat run --network baseSepolia scripts/deploy.ts",
    "demo": "hardhat run --network baseSepolia scripts/demo-flow.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.4",
    "@nomicfoundation/hardhat-verify": "^2.0.3",
    "@types/node": "^20.12.7",
    "ethers": "^6.10.0",
    "hardhat": "^2.20.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
